version: 2

sources:
  - name: citibike_lab_raw.trips
    database: "{{ target.database|upper }}"
    schema: RAW
    tables:
      - name: TRIPS
  - name: citibike_lab_raw.json_weather_data
    database: "{{ target.database|upper }}"
    schema: RAW
    tables:
      - name: JSON_WEATHER_DATA