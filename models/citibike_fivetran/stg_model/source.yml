version: 2

sources:
  - name: raw.trips
    database: "{{ target.database|upper }}"
    schema: RAW
    tables:
      - name: TRIPS
  - name: raw.json_weather_data
    database: "{{ target.database|upper }}"
    schema: RAW
    tables:
      - name: JSON_WEATHER_DATA